// This file is created by me

/**
 * Equivalence:
 * out = 1 if a == b
 *       0 otherwise
 */

 CHIP Equivalence {
	IN a, b;
	OUT out;

	PARTS:
	Xor(a=a, b=b, out=w);
	Not(in=w, out=out);
 }

